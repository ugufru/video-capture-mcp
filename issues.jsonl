{"id": 1, "title": "Project scaffolding and build setup", "priority": 1, "labels": ["setup"], "description": "Create pyproject.toml with all dependencies, src/video_capture_mcp/ package structure, __init__.py, and verify the project builds with uv. Include .gitignore for Python.", "acceptance_criteria": ["pyproject.toml exists with correct dependencies and entry point", "uv sync installs all dependencies without errors", "uv run video-capture-mcp --help or entry point is callable", ".gitignore covers Python artifacts, __pycache__, .venv"], "estimated_scope": "small"}
{"id": 2, "title": "Shared utilities module", "priority": 2, "labels": ["core"], "description": "Create utils.py with make_image_result() helper that builds CallToolResult with ImageContent (base64 PNG), temp directory management (/tmp/video-capture-mcp/), and base64 encoding helpers.", "acceptance_criteria": ["make_image_result() returns valid CallToolResult with ImageContent", "Temp directory is created on demand and cleaned up", "Unit tests pass for base64 encoding and result building"], "estimated_scope": "small", "depends_on": [1]}
{"id": 3, "title": "Camera device listing", "priority": 3, "labels": ["camera", "core"], "description": "Implement camera.py with list_cameras() that probes indices 0-9 via cv2.VideoCapture, returning available devices with index and resolution. Wire up as list_devices MCP tool in server.py with FastMCP.", "acceptance_criteria": ["list_devices tool returns JSON array of available cameras", "Each device entry has index, resolution, and availability", "Probed cameras are released immediately after check", "Server starts and responds to tools/list via stdio"], "estimated_scope": "medium", "depends_on": [1, 2]}
{"id": 4, "title": "Photo capture tool", "priority": 4, "labels": ["camera", "core"], "description": "Implement capture_frame() in camera.py with warm-up frame discard (5 frames for auto-exposure). Wire up as capture_photo MCP tool with device_index, width, height params. Returns base64 PNG via ImageContent.", "acceptance_criteria": ["capture_photo returns a valid PNG image as base64 ImageContent", "Warm-up frames are discarded before capturing final frame", "Optional width/height resizing works", "Error handling for missing/busy camera returns clear message", "Integration test captures from device 0 if available"], "estimated_scope": "medium", "depends_on": [2, 3]}
{"id": 5, "title": "Video capture tool", "priority": 5, "labels": ["camera"], "description": "Implement record_video() in camera.py with cv2.VideoWriter (mp4v codec). Wire up as capture_video MCP tool with duration_seconds (max 30), fps, and return_frames mode that extracts up to 5 evenly-spaced keyframes as ImageContent blocks.", "acceptance_criteria": ["File mode saves MP4 to /tmp/video-capture-mcp/ and returns path", "Frames mode returns up to 5 ImageContent blocks", "Duration is capped at 30 seconds", "Frame extraction selects evenly-spaced frames across the recording", "Integration test records a short clip if camera available"], "estimated_scope": "medium", "depends_on": [4]}
{"id": 6, "title": "OCR tool with preprocessing", "priority": 6, "labels": ["vision"], "description": "Create ocr.py with preprocessing pipeline (auto/threshold/blur/none) for LCD/OLED screen reading. Wire up as ocr_image MCP tool that accepts file path or 'capture' for live frame. Returns extracted text + preprocessed image.", "acceptance_criteria": ["OCR extracts text from a synthetic test image (Pillow-rendered text)", "All 4 preprocessing modes produce output", "Live capture mode works with image_source='capture'", "Returns both extracted text and preprocessed image", "Graceful error if tesseract not installed", "Unit tests pass with synthetic images (no camera needed)"], "estimated_scope": "medium", "depends_on": [2, 4]}
{"id": 7, "title": "Image comparison tool", "priority": 7, "labels": ["vision"], "description": "Implement compare_images() in imaging.py using skimage SSIM. Wire up as compare_images MCP tool. Returns SSIM score, change percentage, bounding boxes of changed regions, and optional diff visualization with red highlights.", "acceptance_criteria": ["SSIM score computed correctly for identical images (1.0) and different images (<1.0)", "Changed regions identified with bounding boxes via contour detection", "Diff visualization highlights changes in red", "Handles images of different sizes by resizing to match", "Unit tests pass with synthetic image pairs (no camera needed)"], "estimated_scope": "medium", "depends_on": [2]}
{"id": 8, "title": "Image manipulation tool", "priority": 8, "labels": ["vision"], "description": "Implement manipulate_image() in imaging.py using Pillow. Supports crop, resize, rotate, annotate, brightness, and contrast operations applied sequentially from an operations list.", "acceptance_criteria": ["Each operation type works correctly (crop, resize, rotate, annotate, brightness, contrast)", "Operations chain sequentially", "Returns processed image as ImageContent + summary text", "Unit tests for each operation type with synthetic images"], "estimated_scope": "medium", "depends_on": [2]}
{"id": 9, "title": "End-to-end tests and error handling", "priority": 9, "labels": ["testing"], "description": "Add comprehensive error handling across all tools (camera not found, camera busy, tesseract missing, file not found, invalid params). Add integration tests that exercise the MCP protocol via stdio. Ensure all error paths return CallToolResult with isError=True.", "acceptance_criteria": ["All error cases return isError=True with descriptive messages", "MCP protocol test sends JSON-RPC via stdio and gets valid responses", "Camera tests skip gracefully when no camera attached", "OCR tests skip gracefully when tesseract not installed", "pytest runs clean with no warnings"], "estimated_scope": "medium", "depends_on": [3, 4, 5, 6, 7, 8]}
{"id": 10, "title": "Documentation and Claude Code configuration", "priority": 10, "labels": ["docs"], "description": "Update README.md with installation instructions (brew install tesseract, uv sync), usage examples for each tool, and Claude Desktop/Code configuration. Create .mcp.json for Claude Code integration.", "acceptance_criteria": ["README has install steps, tool descriptions, and config examples", ".mcp.json is valid and points to the correct entry point", "Claude Desktop config example included"], "estimated_scope": "small", "depends_on": [9]}
{"id": 11, "title": "Add screen capture support", "priority": 11, "labels": ["screen", "core"], "description": "Add screen/display capture tools alongside existing camera capture. Enables capturing what is on screen for local testing of applications like XRoar (Dragon/CoCo emulator) where you need to see what the app is rendering, not just what a physical camera sees.", "acceptance_criteria": ["capture_screen tool captures a screenshot and returns base64 PNG (matching capture_photo format)", "Support display_index for multi-monitor setups", "Support window_name or window_id to capture a specific application window", "Support region parameter (x, y, width, height) for sub-region capture", "Works with existing OCR and compare_images tools", "Screen recording permission handling on macOS with clear error message", "Unit tests with synthetic/mock display"], "estimated_scope": "medium", "depends_on": [2, 4]}
{"id": 12, "title": "Screen capture core module (screen.py)", "priority": 12, "labels": ["screen", "core"], "description": "Create screen.py with list_screens() using Swift/CGGetActiveDisplayList, list_windows() using Swift/CGWindowListCopyWindowInfo, and capture_screen() supporting display_index, window_id, window_name, and region params. Uses PIL.ImageGrab for main display and macOS screencapture CLI for display/window targeting. Detect blank screenshots and raise RuntimeError with screen recording permission instructions.", "acceptance_criteria": ["list_screens() returns display info with index, width, height, is_main", "list_windows() returns window_id, owner, name, bounds for visible windows", "capture_screen() default captures main display via ImageGrab.grab()", "capture_screen(display_index=N) captures specific display via screencapture -D", "capture_screen(window_id=N) captures specific window via screencapture -l", "capture_screen(window_name='XRoar') finds and captures matching window", "capture_screen(region=(x,y,w,h)) captures sub-region", "Blank/all-black screenshot detection with permission error message", "Returns PIL.Image.Image, raises RuntimeError on failure"], "estimated_scope": "medium", "depends_on": [2]}
{"id": 13, "title": "Wire up screen capture MCP tools in server.py", "priority": 13, "labels": ["screen"], "description": "Add three new tools to server.py: list_screens (JSON display info), list_windows (JSON window info), capture_screen (with flat params: display_index, window_id, window_name, region_x/y/width/height). Returns make_image_result(image_to_base64(image)).", "acceptance_criteria": ["list_screens tool returns JSON array of displays", "list_windows tool returns JSON array of windows", "capture_screen tool returns base64 PNG via ImageContent", "Flat region params (region_x, region_y, region_width, region_height) not nested dict", "Error cases return descriptive messages"], "estimated_scope": "small", "depends_on": [12]}
{"id": 14, "title": "Integrate screen source into OCR and compare tools", "priority": 14, "labels": ["screen", "vision"], "description": "Update ocr_image to accept image_source='screen' which captures via screen.capture_screen() instead of camera. Update compare_images_tool load_image() to handle 'screen' path the same way. Enables OCR of emulator output and comparing screen states directly.", "acceptance_criteria": ["ocr_image with image_source='screen' captures screen and runs OCR", "compare_images_tool accepts 'screen' as image path", "Existing 'capture' (camera) and file path modes still work"], "estimated_scope": "small", "depends_on": [12, 6, 7]}
{"id": 15, "title": "Screen capture tests (test_screen.py)", "priority": 15, "labels": ["screen", "testing"], "description": "Create test_screen.py with _screen_capture_available() helper. Mock-based tests that patch PIL.ImageGrab.grab with synthetic images (run everywhere). Hardware-gated tests with @pytest.mark.skipif for real captures. Test invalid window_id and missing window_name raise RuntimeError.", "acceptance_criteria": ["Mock-based tests pass without screen recording permission", "Hardware-gated tests skip gracefully when unavailable", "test_capture_screen_default verifies PIL Image with nonzero dimensions", "test_capture_screen_with_region verifies cropped dimensions", "test_invalid_window_id raises RuntimeError", "test_window_name_not_found raises RuntimeError", "All existing tests still pass"], "estimated_scope": "medium", "depends_on": [12, 13]}
